<div class="wrapper">
  <%= render 'shared/header' %>
  <div class="page-title">
    <h2>Profile</h2>
  </div>

  <%= render 'shared/notifications' %>

  <div class="profile">
  <%= render 'shared/profile' %>

  <% if current_user == @profile.user_id? %>
    <div class="btn">
      <%= link_to "編集", edit_user_profile_path(params[:id]) %>
    </div>
  <% end %>
</div>

<%= render 'shared/footer'%>

</div>
